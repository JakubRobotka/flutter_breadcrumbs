import 'package:flutter/material.dart';

class DoubleNestedItemPage extends StatelessWidget {
  const DoubleNestedItemPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("double nested"),
    );
  }
}
